; ESP32 RF Probe & Path Loss Analyzer – build/upload
; Use with: pio run | pio run -t upload | pio device monitor -b 115200

[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200

; Optional: use a specific core version (uncomment if you need it)
; platform_packages = platformio/framework-arduinoespressif32 @ https://github.com/espressif/arduino-esp32/releases/download/...
